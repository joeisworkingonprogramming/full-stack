{
  "name": "full-stack",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build:ui": "rm -rf backend/public && cd frontend && npm run build && cp -r build/ ../backend/public/ && rm -rf build",
    "push:all": "npm run build:ui && git add . && git commit -m update && git push"
  },
  "author": "joe",
  "license": "ISC"
}
