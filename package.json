{
  "name": "full-stack",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build:ui": "rm -rf backend/build && cd frontend && npm run build && cp -r build ../backend && rm -rf frontend/build",
    "deploy:full": "npm run build:ui && git add . && git commit -m update && git push"
  },
  "author": "joe",
  "license": "ISC"
}
