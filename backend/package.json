{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf public && cd ../frontend && npm run build && cp -r build/ ../backend/public/ && rm -rf build",
    "start": "node server.js",
    "dev": "nodemon --inspect server.js"
  },
  "author": "joe",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^6.10.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
